[tox]
envlist=py3
skipsdist=False

[testenv]
deps = -r{toxinidir}/requirements_tests.txt
commands = pytest --cov --cov-branch --ignore=isoquant_tests/console_test.py

[coverage:run]
omit = venv/*, .tox/*, isoquant_tests/*, setup.py
